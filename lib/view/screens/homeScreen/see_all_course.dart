import 'package:flutter/material.dart';
class SeeAllCourse extends StatelessWidget {
  const SeeAllCourse({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
